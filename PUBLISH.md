Bump version in:
- package.json
- projects/ng-select/package.json
- src/app/components/home.compenent.ts

Build with:
```bash
yarn run build-lib
```

Publish from dist directory:
```bash
cd dist/ng-select
npm publish
```
